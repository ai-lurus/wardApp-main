// Warden fallback card — renders when the server emits an unknown cardType
// or when Zod validation fails on the client. Implementation lands in T056 (US1).
export default function FallbackCard() {
  return null;
}
