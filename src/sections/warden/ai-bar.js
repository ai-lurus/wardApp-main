// Warden AI bar — persistent entry point rendered in the main layout.
// Opens the Warden panel and surfaces unread alerts.
// Implementation lands in T046 (US1). Component must be JS (no TS).
export default function AiBar() {
  return null;
}
